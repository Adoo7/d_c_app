import 'package:flutter/material.dart';

// Primary Colors
const primaryColor = Color.fromRGBO(13, 71, 161, 1); // Dark Industrial Blue
const primaryColorLight =
    Color.fromRGBO(25, 118, 210, 1); // Lighter Industrial Blue
const primaryColorDark = Color.fromRGBO(1, 67, 97, 1); // Darker Industrial Blue

// Text Colors
const lightTextColor = Colors.white;
const darkTextColor = Color.fromRGBO(33, 33, 33, 1); // Dark Gray for text

// Surface Colors
const surfaceColor = Colors.white;
const surfaceColorDark = Color.fromRGBO(18, 18, 18, 1); // Dark background
const onSurfaceColor = Color.fromRGBO(33, 33, 33, 1); // Text on surface
const onSurfaceColorDark = Colors.white; // Text on dark surface

// Accent Colors
const secondaryColor = Color.fromRGBO(0, 151, 167, 1); // Teal accent
const secondaryColorDark = Color.fromRGBO(0, 121, 137, 1); // Darker teal
const onSecondaryColor = Colors.white;
const onSecondaryColorDark = Colors.white;

// Error Colors
const errorColor = Color.fromRGBO(211, 47, 47, 1); // Material Red
const onErrorColor = Colors.white;

// Card Colors
const cardColor = Colors.white;
const cardColorDark = Color.fromRGBO(30, 30, 30, 1); // Dark card background
